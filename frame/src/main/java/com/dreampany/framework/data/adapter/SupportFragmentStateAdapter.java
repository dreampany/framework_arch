/*
package com.dreampany.framework.data.adapter;


import android.support.v4.app.Fragment;
import android.support.v4.app.FragmentManager;

public class SupportFragmentStateAdapter<T extends Fragment> extends BaseSupportFragmentStateAdapter<T> {

    private SupportFragmentStateAdapter(FragmentManager fragmentManager) {
        super(fragmentManager);
    }

    public static SupportFragmentStateAdapter<?> newAdapter(FragmentManager fragmentManager) {
        return new SupportFragmentStateAdapter<>(fragmentManager);
    }

    @Override
    public Fragment getItem(int position) {
        return newFragment(position);
    }
}
*/
